manifestVersion: 1
name: "@keeb/mongodb"
version: "2026.04.06.1"
description: "MongoDB integration (queries, inserts, connectivity diagnostics, user search, related record lookup)"
repository: https://github.com/keeb/swamp-mongodb
labels:
  - mongodb
  - database
  - nosql
models:
  - mongodb_query.ts
skills:
  - mongodb
